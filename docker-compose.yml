version: '3'
services:
  redis:
    image: redis
    container_name: redis
    command: redis-server --maxmemory 4gb --maxmemory-policy allkeys-lru --timeout 0 --maxclients 10000 --save "" --appendonly no
    ports:
      - "6379:6379"
    restart: always
